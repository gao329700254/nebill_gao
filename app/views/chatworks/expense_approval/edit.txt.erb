<% to_user.each do |user| %>
  [To:<%= user.chatwork_id %>] <%= user.chatwork_name %>さん
  
<% end %>
[info]
  [title]
    <%= expense_approval.created_user.name %>さんから経費申請「<%= expense_approval.name %>」が修正されました.
  [/title]
  [info]
    <%= expense_approval.notes %>

    以下のリンクをクリックして,内容を確認ください.
    <%=  Rails.application.routes.url_helpers.expense_approval_show_url(expense_approval, ActionMailer::Base.default_url_options) %>
  [/info]
[/info]