[To:<%= to_user.chatwork_id %>] <%= to_user.chatwork_name %>さん
[info]
  [title]
    取引先申請「<%= approval.name %>」が承認されました
  [/title]
  [info]
    <% approval.approval_users.with_permission.each do |approval_user| %>
      <%= approval_user.user.name %> ： <%= approval_user.comment %>
    <% end %>

  　現在、NDAのみの承認となっております.　基本契約書を添付の上、更新してください.
    以下のリンクをクリックして,内容を確認ください.
    <%=  Rails.application.routes.url_helpers.client_show_url(approval, ActionMailer::Base.default_url_options) %>
  [/info]
[/info]
