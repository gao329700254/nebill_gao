[To:<%= to_user.chatwork_id %>] <%= to_user.chatwork_name %>さん
[info]
  [title]
    <%= approval.created_user.name %>さんから「<%= approval.name %>」が承認申請されました.
  [/title]
  [info]
    <%= approval.notes %>

    以下のリンクをクリックして,内容を確認ください.
    <%=  Rails.application.routes.url_helpers.approval_show_url(approval, ActionMailer::Base.default_url_options) %>
  [/info]
[/info]
